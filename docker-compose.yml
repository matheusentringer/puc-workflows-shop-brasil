# =============================================================================
# Lab: Orquestração de Workflows — Aula 1
# Apache Airflow 2.x + PostgreSQL (metadata DB + destino do ETL)
# =============================================================================
# USO:
#   docker compose up -d          → sobe todos os serviços
#   docker compose ps             → verifica saúde dos containers
#   docker compose logs -f        → acompanha logs em tempo real
#   docker compose down -v        → derruba e apaga volumes
#
# UI do banco Shop (Adminer): http://localhost:8081
#   Sistema: PostgreSQL | Servidor: postgres-shop | Usuário: shop
#   Senha: shop123 | Base: shopdb
# =============================================================================

x-airflow-common: &airflow-common
  image: apache/airflow:2.9.3-python3.11
  environment: &airflow-common-env
    AIRFLOW__CORE__EXECUTOR: LocalExecutor
    AIRFLOW__CORE__DEFAULT_TIMEZONE: "America/Sao_Paulo"
    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres-meta/airflow
    AIRFLOW__CORE__FERNET_KEY: "46BKJoQYlPPOexq0OhDZnIlNepKFf87WFwLt0nfd7cA="
    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
    AIRFLOW__CORE__LOAD_EXAMPLES: "false"
    AIRFLOW__WEBSERVER__SECRET_KEY: "shop-secret-key-2024"
    AIRFLOW__WEBSERVER__EXPOSE_CONFIG: "true"
    AIRFLOW__API__AUTH_BACKENDS: "airflow.api.auth.backend.basic_auth"
    # Provider de PostgreSQL para o lab
    _PIP_ADDITIONAL_REQUIREMENTS: "apache-airflow-providers-postgres==5.11.0 pandas==2.2.2 requests==2.32.3"
  volumes:
    - ./dags:/opt/airflow/dags
    - ./logs:/opt/airflow/logs
    - ./plugins:/opt/airflow/plugins
    - ./data:/opt/airflow/data
  depends_on:
    postgres-meta:
      condition: service_healthy
  networks:
    - airflow-net

services:
  # ── Banco de dados de metadados do Airflow ──────────────────────────────────
  postgres-meta:
    image: postgres:16
    container_name: airflow-meta-db
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow
      PGTZ: America/Sao_Paulo
      TZ: America/Sao_Paulo
      
    volumes:
      - postgres-meta-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "airflow"]
      interval: 10s
      retries: 5
      start_period: 5s
    networks:
      - airflow-net

  # ── Banco de dados DESTINO do ETL (onde o DAG vai inserir dados) ────────────
  postgres-shop:
    image: postgres:16
    container_name: airflow-shop-db
    environment:
      POSTGRES_USER: shop
      POSTGRES_PASSWORD: shop123
      POSTGRES_DB: shopdb
      PGTZ: America/Sao_Paulo
      TZ: America/Sao_Paulo
    volumes:
      - postgres-shop-data:/var/lib/postgresql/data
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5433:5432"        # acessível em localhost:5433 para inspeção externa
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "shop", "-d", "shopdb"]
      interval: 10s
      retries: 5
      start_period: 5s
    networks:
      - airflow-net

  # ── UI web para visualizar o banco shopdb (Adminer) ─────────────────────────
  adminer:
    image: adminer:4.8.1
    container_name: shop-db-admin
    ports:
      - "8081:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres-shop
      ADMINER_DESIGN: pepa-linha
    depends_on:
      postgres-shop:
        condition: service_healthy
    networks:
      - airflow-net
    restart: unless-stopped

  # ── Inicialização do banco de metadados (roda uma vez e sai) ────────────────
  airflow-init:
    <<: *airflow-common
    container_name: airflow-init
    entrypoint: /bin/bash
    command:
      - -c
      - |
        airflow db migrate
        airflow users create \
          --username admin \
          --firstname Admin \
          --lastname Shop \
          --role Admin \
          --email admin@shop.local \
          --password admin
        airflow connections add postgres_shop \
          --conn-type postgres \
          --conn-host postgres-shop \
          --conn-login shop \
          --conn-password shop123 \
          --conn-schema shopdb \
          --conn-port 5432
        echo "=== Airflow inicializado. Acesse http://localhost:8080 ==="
    environment:
      <<: *airflow-common-env
      _AIRFLOW_DB_MIGRATE: "true"
      _AIRFLOW_WWW_USER_CREATE: "true"
    depends_on:
      postgres-meta:
        condition: service_healthy
      postgres-shop:
        condition: service_healthy

  # ── Scheduler ───────────────────────────────────────────────────────────────
  airflow-scheduler:
    <<: *airflow-common
    container_name: airflow-scheduler
    command: scheduler
    healthcheck:
      test: ["CMD", "airflow", "jobs", "check", "--job-type", "SchedulerJob", "--allow-multiple", "--limit", "100"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  # ── Webserver (UI) ──────────────────────────────────────────────────────────
  airflow-webserver:
    <<: *airflow-common
    container_name: airflow-webserver
    command: webserver
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

volumes:
  postgres-meta-data:
  postgres-shop-data:

networks:
  airflow-net:
    driver: bridge
